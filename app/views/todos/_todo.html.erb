<%= content_tag :tr, id: "todo-#{todo.id}" do %>
	<td><%= link_to todo.description, todo %></td>
	<td><%= todo.days_left %></td>
	<td>
		<%= link_to "Delete", todo, method: :delete, confirm: 'Are you sure you want to delete this item?', remote: true %>
	</td>
</tr>
<% end %>
